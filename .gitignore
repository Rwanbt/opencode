.DS_Store
node_modules
*.keystore
packages/mobile/src-tauri/ort-android/
.worktrees
.sst
.env
.idea
.vscode
.codex
*~
playground
tmp
dist
ts-dist
.turbo
**/.serena
.serena/
/result
refs
Session.vim
/opencode.json
a.out
target
.scripts
.direnv/

# Local dev files
opencode-dev
UPCOMING_CHANGELOG.md
logs/
*.bun-build
tsconfig.tsbuildinfo

# Android NDK toolchain config (machine-specific paths)
packages/mobile/src-tauri/.cargo/config.toml

# Mobile runtime assets downloaded by scripts/prepare-android-runtime.sh
# (aarch64-linux-musl bun/bash/rg binaries + ld-musl + libstdc++/libgcc libs).
# These are fetched at build time, machine-architecture specific, and too big
# to commit — they must be regenerated by running prepare-android-runtime.sh.
packages/mobile/src-tauri/assets/runtime/bin/
packages/mobile/src-tauri/assets/runtime/lib/

# Non-workspace helper crates have their own Cargo.lock we don't need to
# track (they're always consumed from the workspace root lockfile).
crates/opencode-kokoro-shared/Cargo.lock

# Local LLM eval results
packages/opencode/test/tool/eval-results/

# Claude Code / OpenCode agent workspace
.claudeignore
.opencode/

# Dev scratch files (Rust examples, test snippets)
packages/desktop/src-tauri/examples/

# Secrets & signing (B.DOC from audit 2026-04-17)
*.pem
*.p8
*.pfx
*.jks
*.p12
.env.local
.env.*.local
.env.production
gradle.properties
local.properties
keystore.properties
**/secrets/**
# Keep template files
!keystore.properties.example
!gradle.properties.template

# Benchmark artefacts (audit §7)
packages/opencode/bench/results/
*.bench.json

# OS / IDE
Thumbs.db
ehthumbs.db

# Alpine rootfs binary archive (generated by build-alpine-rootfs.sh, ~72MB)
packages/mobile/src-tauri/assets/runtime/rootfs.tgz
packages/mobile/src-tauri/gen/android/app/src/main/assets/runtime/rootfs.tgz
packages/mobile/src-tauri/gen/android/app/src/main/assets/runtime/rootfs_version.txt
