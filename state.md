### DECISIONS
- Treat full-reload instability as distinct from the 10 x 100 prompt-cost problem | `10 x 0` fails before prompts run | increasing timeout or reducing workload | active
- Preserve `/automate` while the Workbench grant is unresolved | a boolean conflated pending and denied access | fallback to Code before grant resolution | active
- Fence Workbench connection completion by provider generation | a disposed provider must not publish a late connection | rely only on lifecycle cleanup | active
- Set the certification timeout to 35 minutes | the measured sequential backend cost is about 1.57 s per prompt | reduce the 10 x 100 workload or use an arbitrary timeout | active
- Make workflow authority explicit at every production boundary | start creates and returns the canonical AuthorityToken; resume, inspect, history, cancel, and worker complete consume it; no runtime or HTTP ownership map | retain run-ID-only APIs or auto-claim after restart | active
- Promote Automate through five stacked slices, not one historical cherry-pick range | the early foundation range changes 502 files and mixes multiple responsibilities | a single promotion PR or automatic range cherry-pick | active
- Keep dev outside the Automate promotion | owner instruction | using dev as a base or target for any slice | active
- Promote Automate as six stacked local slices A1/A2/A3/A4/A5a/A5b, never a monolithic PR | A5b tip `323e5e0e8c` is parity-identical to source except six documented divergences | single PR or wholesale cherry-pick | active
- Keep the `workflow-ir.ts` split (`1193 + 128 + 363` lines) in every slice | source monolith is 1794 lines, over the 1500 mandatory ceiling; 77 public exports verified identical | promote the monolith | active
### UNCERTAINTIES
- What resource previously reached Chromium's `ERR_INSUFFICIENT_RESOURCES` during repeated full reloads | no longer blocks certification because the bootstrap gate passes the full workload, but the underlying browser-resource mechanism was not isolated | reproduce without the bootstrap gate and inspect browser resource accounting | P2
- Whether `page.goto()` full documents are the intended certification model rather than a harness artifact | the test contract explicitly describes reloads but its resource metric is document-local | inspect original certification intent and compare a fresh-page reproduction | P1

### VERIFIED FINDINGS
- GraphRuntimeEngine durable transitions now require explicit shared AuthorityToken fencing, initialize workflow_authority, and reject cross-run/stale tokens | packages/workflow-runtime/src/graph-runtime.ts; packages/workflow-runtime/src/authority.ts; packages/workflow-runtime/test/graph-runtime.test.ts | `bun test test/graph-runtime.test.ts` and `bun x tsc --noEmit` from packages/workflow-runtime -> 24 pass, 0 fail, typecheck clean | confirmed
- NativeWorkflowRuntimePort claims and threads durable graph authority tokens | packages/workbench-server/src/native-workflow-port.ts | `bun run typecheck` and targeted native workflow/HTTP tests -> typecheck clean, 4 pass, 0 fail | confirmed
- Targeted mode/provider tests pass | packages/app/src/context/mode.test.ts; packages/app/src/context/mode-directory.test.ts; packages/app/src/context/workbench/provider.test.ts | `bun test --preload ./happydom.ts ./src/context/mode.test.ts ./src/context/mode-directory.test.ts ./src/context/workbench/provider.test.ts` -> 49 pass, 0 fail | confirmed
- App typecheck passes after the tri-state and generation changes | packages/app | `bun run typecheck` -> exit 0 | confirmed
- Repeated reloads fail without prompt load | packages/app/e2e/modes/mode-reload-stability.spec.ts | `E2E_RELOAD_CYCLES=10 E2E_RELOAD_PROMPTS=0 bun run test:e2e -- --workers=1 e2e/modes/mode-reload-stability.spec.ts` -> missing Work/Automate mode projection | confirmed
- A failing reload emitted `ERR_INSUFFICIENT_RESOURCES` for a Vite module after cancelled backend/Vite requests | prior instrumented reproduction of mode-reload-stability | Playwright browser event output observed `net::ERR_INSUFFICIENT_RESOURCES` for `hover-card.tsx` | confirmed
- Disabling the global SDK event-start in this spec did not stabilize `10 x 0` | packages/app/src/context/global-sync.tsx temporary diagnostic, reverted | same `10 x 0` failure persisted | confirmed
- The bootstrap marker makes the zero-prompt reload gate stable | packages/app/src/pages/workbench-mode.tsx | `E2E_RELOAD_CYCLES=10 E2E_RELOAD_PROMPTS=0 E2E_RELOAD_MODES=design,automate,work bun run test:e2e -- --workers=1 e2e/modes/mode-reload-stability.spec.ts` -> 1 pass twice | confirmed
- The full 10 x 100 reload contract passes | packages/app/e2e/modes/mode-reload-stability.spec.ts | `bun run test:e2e -- --workers=1 e2e/modes/mode-reload-stability.spec.ts` -> 1 pass in 34.3m | confirmed
- The prompt backend is the long-run cost | packages/app/e2e/modes/mode-reload-stability.spec.ts | `E2E_RELOAD_CYCLES=1 E2E_RELOAD_PROMPTS=10 E2E_RELOAD_DIAGNOSTICS=1 bun run test:e2e -- --workers=1 e2e/modes/mode-reload-stability.spec.ts` -> prompt request 15,667 ms, response 26 ms, idle 450 ms | confirmed
- NativeAttemptAuthority rejects unknown effects and requires explicit retry authorization after reconciled failure | packages/workflow-runtime/src/native-attempts.ts; packages/workflow-runtime/test/native-durable.test.ts | `bun test` from packages/workflow-runtime -> 116 pass, 0 fail; `bun x tsc --noEmit` -> exit 0 | confirmed
- Durable history protected mutations now require token-first APIs and native SQLite fencing | packages/workflow-runtime/src/adapter.ts; packages/workflow-runtime/src/native-history.ts; packages/workflow-runtime/src/authority.ts | `bun x tsc --noEmit` -> exit 0; `bun test` from packages/workflow-runtime -> 117 pass, 0 fail | confirmed
- Native workflow port isolates concurrent runs and pins immutable versions across restart/publication | packages/workbench-server/src/native-workflow-port.ts; packages/workbench-server/test/native-workflow-port.test.ts; packages/workbench-server/test/workflow-http.test.ts | `bun test test/native-workflow-port.test.ts test/workflow-http.test.ts` -> 4 pass, 0 fail; `bun run test` -> 24 + 53 pass, 0 fail; `bun run typecheck` -> exit 0 | confirmed
- Production HTTP workflow routes now consume opaque run IDs returned by start | packages/workbench-server/test/workflow-http.test.ts; packages/workbench-server/test/e2e-full-journey.test.ts | targeted Bun test passed; Vitest E2E cannot load `bun:sqlite` and returns 400 before the port executes | confirmed
- NativeApprovalAuthority takeover tests pass the current AuthorityToken after the breaking signature change | packages/workflow-runtime/test/native-durable.test.ts:184,241 | `bun test test/approval-v4.test.ts test/native-durable.test.ts` from packages/workflow-runtime -> 50 pass, 0 fail; `bun x tsc --noEmit` -> exit 0 | confirmed
- Workbench workflow authority contract now requires shared AuthorityToken and keeps inspect read-only | packages/workbench-server/src/workflow-port.ts; packages/workbench-server/src/native-workflow-port.ts; packages/workbench-server/src/handlers/workflows.ts; packages/workbench-server/src/handlers/automation.ts | `bun run typecheck` and `bun test test/native-workflow-port.test.ts test/workflow-http.test.ts test/server.test.ts` from packages/workbench-server -> typecheck clean, 4 workflow tests pass, server integration 87/87 pass | confirmed
- Native workflow authority no longer has an internal token/owner map and restart uses the returned canonical token | packages/workbench-server/src/native-workflow-port.ts; packages/workbench-server/src/server.ts; packages/workbench-server/src/server-context.ts | repository grep for `workflowOwners`, `tokens.get(runId)`, and synthetic workbench claims -> no matches | confirmed
- Workflow graph authority assertion is reused for protected history reads | packages/workflow-runtime/src/graph-runtime.ts:123-126; packages/workbench-server/src/native-workflow-port.ts:95-100 | `bun test test/graph-runtime.test.ts` from packages/workflow-runtime -> 24 pass, 0 fail | confirmed
- Full workflow journey executes under Bun but cleanup has an existing Windows SQLite lock failure | packages/workbench-server/test/e2e-full-journey.test.ts:137 | `bun test test/e2e-full-journey.test.ts` -> journey A behavior completed, journey B passed, journey A failed only with EBUSY removing temp directory; Vitest cannot resolve `bun:sqlite` | confirmed
- Canonical authority production-path E2E covers one HTTP-started run across graph, history, attempts, approvals, and cancellation | packages/workbench-server/test/canonical-authority-e2e.test.ts | `bun test test/canonical-authority-e2e.test.ts` -> 1 pass, 0 fail, 31 expect() calls; `bun run typecheck` -> exit 0 | confirmed
- Canonical authority survives subsystem restart after generation-5 takeover | packages/workbench-server/test/canonical-authority-e2e.test.ts:138-205 | `bun test test/canonical-authority-e2e.test.ts` -> 1 pass, 0 fail, 38 expect() calls | confirmed
- Promotion topology is not ready for a monolithic dev integration | origin/work-design and origin/dev versus origin/agent/automate-v2-baseline-20260901 | `git rev-list --left-right --count`: work-design `0/315`, dev `3/551`; dev diff `977 files, 149357 insertions, 15685 deletions` | confirmed
- Automate promotion has five dependency-ordered review slices | docs/integration/AUTOMATE-PROMOTION-SLICES.md | source diff ranges: foundation `502 files/82936 additions`, runtime `11 files/1745 additions`, HTTP `15 files/1001 additions`, hardening `50 files/1790 additions` | confirmed
- A5b tip is parity-identical to `origin/agent/automate-v2-baseline-20260901` except six deliberate divergences | `git diff HEAD origin/agent/automate-v2-baseline-20260901` in automate-a5b-app -> 14 files: roadmap, IR split, 4 bespoke tests, ownership test, canonical E2E 1-line fix, state.md, 2 README headers | confirmed
- Source canonical E2E has a latent TS2353 at line 192 (`{ ok: true }` vs `{ result?, ackLost? }`) | `bun x tsc --noEmit` in BOTH automate-a4-workbench (pre-fix) and design worktree source -> identical error; Bun test stays green because Bun does not typecheck | confirmed
- Prior claim `workbench-server typecheck clean` is therefore REJECTED for the final source state; it held only before the restart-fencing section was added | re-ran `bun x tsc --noEmit` on source 2026-09-06 -> 1 error | REJECTED (superseded)
- Slice re-verification totals on 2026-09-06: contracts 644/644, workflow-runtime 118/118, workbench-server suite 24+53 plus workflow 11/11, app targeted 49/49, foundations 49+33+5+16+17 | exact `bun test`/`typecheck` runs per slice worktree, all 0 fail | confirmed
- `dev` was never touched and nothing was pushed or merged during slicing | branch inspection per worktree; D: disk-full forced `node_modules` deletion on sealed A1-A4 worktrees (reinstallable) | confirmed
- All six promotion slices plus the Automate baseline pushed to origin on 2026-09-06 | a1 `ff9fa1bfd1`, a2 `0208882bfd`, a3 `189d6a6792`, a4 `012d893d90`, a5-foundations `bcd8b8d350`, a5b-app `323e5e0e8c`, baseline `1a1e8e1ecd`; `git ls-remote origin` matches local SHAs on all 7 refs | `git push origin <7 branches>` -> pre-push `turbo typecheck` 47/47 green after the 1-line canonical-E2E test-only fix; remote reports 58 Dependabot vulns on default branch (informational) | confirmed
